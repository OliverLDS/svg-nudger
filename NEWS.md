# News

## v3.3

- Simplified the left panel layout to save space.
- Added `SVG Document` help with the Safari PDF export workflow note.
- Changed the practical Safari print scale note from `345%` to `340%`.
- Extended path endpoint editing to simple line paths such as `M ... L ...`.

## v3.2

- Added draggable path end points, curve controls, arc handles, and arc-center handles.
- Added a toggle to show or hide path editing handles.
- Added a visual legend for path edit handles.

## v3.1

- Defaulted nested `<tspan>` clicks to the parent `<text>` element.
- Added `Option` / `Alt` + click for exact `<tspan>` selection.

## v3

- Initial tagged release.
- Added direct SVG code editing, export actions, text styling, and selection/nudging workflows.
